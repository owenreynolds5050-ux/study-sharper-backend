This is a test document for LangChain extraction.

It contains multiple paragraphs to test the chunking functionality.

The LangChain processor should split this into chunks and generate embeddings for each one.

Let's see if it works correctly!